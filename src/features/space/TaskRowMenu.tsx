import type { PopoverPos } from '../../components/ui/Popover'
import type { Task } from '../../lib/types'

export function TaskRowMenu({
  task,
  pos,
  onClose,
}: {
  task: Task
  pos: PopoverPos
  onClose: () => void
}) {
  void task, void pos, void onClose
  return null
}

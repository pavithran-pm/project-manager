import type { AppNotification } from '../../lib/types'

export function NotificationDetail({
  notification,
  onBack,
}: {
  notification: AppNotification
  onBack: () => void
}) {
  void notification, void onBack
  return null
}
